<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        x:Class="MyAdminExplorer.ControlUser"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MyAdminExplorer — Users" Height="450" Width="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <Button Content="Back" Click="Back" Margin="0,0,12,0"/>
            <Label VerticalAlignment="Center" FontSize="20" Content="User management"/>
        </DockPanel>
        <DataGrid CellEditEnding="EditEnding" Grid.Row="1" x:Name="dataGrid" AutoGenerateColumns="True" Margin="0,0,0,12"/>
        <UniformGrid Grid.Row="2" Columns="4">
            <Button Content="Add" Click="Add" Margin="4"/>
            <Button Content="Edit folders" Click="Edit_Folders" Margin="4"/>
            <Button Content="Save" Click="SaveUser" Margin="4"/>
            <Button Content="Delete" Click="DeleteUser" Margin="4"/>
        </UniformGrid>
    </Grid>
</Window>
