<Window x:Class="MyAdminExplorer.EditForbidden"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:infra="clr-namespace:MyAdminExplorer.Infrastructure"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MyAdminExplorer — Blocked Folders" Height="500" Width="700" Loaded="Window_Loaded">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TreeView x:Name="FoldersItem" Grid.RowSpan="3" SelectedItemChanged="foldersItem_SelectedItemChanged">
            <TreeView.Resources>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Width="20" Height="20" Stretch="Fill"
                                           Source="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TreeViewItem}}, Path=Header, Converter={x:Static infra:HeaderToImageConverter.Instance}}" />
                                    <TextBlock Text="{Binding}" Margin="5,0" />
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.Resources>
        </TreeView>
        <Label Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" FontSize="20" Content="Blocked folders"/>
        <ListView Grid.Row="1" Grid.Column="1" x:Name="ForbiddenList"/>
        <StackPanel Grid.Row="2" Grid.Column="1">
            <Button Content="Remove" Click="Delete" Margin="0,0,0,8"/>
            <Button Content="Close" Click="CloseWindow"/>
        </StackPanel>
    </Grid>
</Window>
