<Window x:Class="MyAdminExplorer.ChangePass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Closed="Window_Closed"
        WindowStartupLocation="CenterScreen"
        Title="MyAdminExplorer — Change Password" Height="280" Width="420">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" FontSize="16" Content="New password" Margin="0,0,0,8"/>
        <PasswordBox Grid.Row="1" FontSize="16" x:Name="password" Margin="0,0,0,16"/>
        <Label Grid.Row="2" VerticalAlignment="Top" FontSize="16" Content="Confirm password" Margin="0,0,0,8"/>
        <PasswordBox Grid.Row="2" VerticalAlignment="Bottom" FontSize="16" x:Name="confirmpassword" Margin="0,24,0,16"/>
        <Button Grid.Row="3" Click="Change" Content="Change password" Margin="0,0,0,8"/>
        <Button Grid.Row="4" Click="CloseWindow" Content="Close"/>
    </Grid>
</Window>
